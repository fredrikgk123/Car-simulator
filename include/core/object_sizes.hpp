#pragma once

/**
 * Standard object dimensions.
 * Shared between physics and rendering.
 */
namespace ObjectSizes {

inline constexpr float VEHICLE_WIDTH = 1.0f;
inline constexpr float VEHICLE_HEIGHT = 0.5f;
inline constexpr float VEHICLE_LENGTH = 2.0f;

inline constexpr float WALL_LENGTH = 5.0f;
inline constexpr float WALL_THICKNESS = 2.0f;

inline constexpr float TREE_COLLISION_RADIUS = 0.5f;
inline constexpr float TREE_HEIGHT = 6.0f;

inline constexpr float POWERUP_SIZE = 0.8f;

} // namespace ObjectSizes
